spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_clientes_personas
    username: root
    password: 21061993
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  application:
    name: cuentas_movimientos
server:
  port: 8082